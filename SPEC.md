# SPEC：Safe Coding Agent Harness

## 1. 问题与目标

LLM 能生成 coding 计划，却不能直接承担文件修改、命令执行和测试反馈的工程责任。本项目实现一个最小可运行 harness，将 LLM 决策与确定性的工具、护栏、反馈、记忆和停机逻辑分离。

目标用户是希望在本地项目中安全尝试 coding agent 的个人开发者和学生。价值在于：危险动作可被代码拦截，失败结果可回灌，核心机制可在没有真实 LLM 和网络的情况下测试。

## 2. 用户故事

1. 作为开发者，我希望 agent 能读取和修改工作区文件，以完成小型 coding 任务。
2. 作为开发者，我希望 agent 能运行测试并收到失败输出，以便改变下一步动作。
3. 作为开发者，我希望危险命令在执行前被确定性拦截。
4. 作为开发者，我希望 agent 的关键反馈能持久化，便于下一次运行了解历史。
5. 作为维护者，我希望用 mock LLM 离线验证核心机制，而不依赖模型随机性。

## 3. 功能模块

### 决策循环
输入目标，调用可注入 LLM，解析 `tool/final/stop` 动作，执行并在达到最终动作或步数上限时停机。

### 工具分发
提供 `read_file`、`write_file`、`run_command` 三个工具；未知工具返回结构化失败结果。

### 治理护栏
危险命令拦截；文件路径必须位于工作区内。护栏在工具执行之前运行，不能由提示词绕过。

### 客观反馈
工具退出码和异常被转换为确定性的成功/失败反馈，并追加到下一轮消息。

### 记忆
记录工具反馈和被拦截动作；可写入 JSON 文件并按关键词检索。

### 配置与停机
运行时可设置工作区、最大步数和危险命令集合；可从 JSON 规则文件加载。最终动作、显式 stop 或达到最大步数均会停机。

## 4. 领域与机制设计

Coding 领域的客观反馈是命令退出码、测试输出和文件结果；危险动作是删除、格式化、关机、越界写文件；所需工具是文件读写和测试/命令执行；记忆需求是失败反馈和被拦截策略。主要贡献选择治理与反馈闭环，因为它们能完全通过代码和 mock LLM 确定性验证，而不是依赖提示词服从。

## 5. 非功能需求

- 无网络 mock 测试必须可重复运行。
- 单轮默认最多 8 步，防止无限循环。
- 工具异常转为反馈，不直接导致 harness 崩溃。
- API key 不进入源码、日志和测试；当前 demo 不需要 key。

## 6. 架构与数据流

`LLMClient → AgentLoop → Guardrail → ToolRegistry → FeedbackValidator → AgentLoop`。
Memory 接收每次反馈并可持久化。LLM 只负责提出动作，工具和护栏由本项目代码负责。

## 7. 技术选择与分发

使用 Python 3.10+，因为标准库足够实现 CLI、子进程和 JSON 持久化，便于个人用户安装。项目以 wheel 包和 Git Release 分发；mock demo 无需 API key，真实供应商接入必须使用环境变量或操作系统凭据管理器。

## 8. 验收标准

- 危险命令测试必须稳定失败并且不执行命令。
- 工具失败后，下一动作必须被调用且可以不同于失败动作。
- mock LLM 下能完成一次“失败 → 反馈 → 修正 → 完成”。
- 在新环境先执行 `python -m pip install -e ".[dev]"` 后，`pytest` 一键通过；CLI demo 可重复运行。

## 9. 风险与未决问题

shell 在不同操作系统上语义不同；当前版本不是完整 sandbox。真实 LLM 适配、Windows/macOS/Linux 命令差异和人工审批 UI 留作后续迭代。
