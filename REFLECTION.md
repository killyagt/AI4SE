# 项目反思

## 一、实际运行与安全机制

我首先在命令行里运行了项目自带的演示。演示程序自动复现了危险命令被拦截、安全写文件成功的过程。另外，自动化测试还验证了使用 `../` 访问上级目录时会被拒绝。这个过程很直观，让我知道 Harness 并不是简单地把所有动作都丢给 AI 去判断，而是自己就守住了安全边界。

随后我运行了自动化测试，屏幕上显示 `11 passed`，说明程序对危险命令拦截、路径保护、结果反馈这些关键行为的处理，都符合预期。这说明安全机制有实际用例验证。

我还专门打开了 `guardrails.py`，看到里面在真正执行工具动作之前，会先对命令和文件路径做检查。在这个项目的设计中，LLM 或 mock LLM 负责根据当前状态提出下一步动作，而 Harness 会检查这个动作是否安全：安全就执行并返回结果，不安全就拦截并反馈回去。这种“AI 提想法，Harness 当安全守门人”的职责分离设计，比让 AI 自己检测危险更可靠，也让我对如何给 AI 系统加上可控约束有了更具体的理解。

## 二、TDD 和开发流程

“先写设计、再写计划、再先写失败测试”的流程，对我最大的帮助就是拆掉了上来就做的冲动。先写设计文档能梳理目标，再写计划能把大任务拆成小步骤，不至于卡在某个地方不知道下一步该做什么。

先写失败测试这一步，给我的感觉更像是在给自己设路标。测试红的时候，我很清楚代码还缺什么；实现逻辑完成后看到测试变绿，反馈特别直接。跑全量测试时看到 11 个测试通过，也能确认新功能没有破坏旧逻辑。

让我觉得形式感比较强的地方，是设计文档里对已有代码的复述。Harness 已经有现成的类结构了，但设计文档还要把方法和职责再描述一遍。对于一个人做的小练习，这有时像是在抄代码。不过后来我意识到，这份文档是给没有读过源码的协作者看的，能帮助别人快速理解整体结构。

## 三、AI 协作中的分工

AI 帮助我完成了主要代码初稿、测试用例、文档框架和概念解释。在我对某些概念理解有偏差时，AI 也帮助我理清了 Harness 和 LLM 各自的职责边界。

我自己选择了安全护栏作为重点，实际运行了项目自带的 mock 演示和自动化测试，观察安全拦截和安全操作的输出；我还打开 `guardrails.py` 阅读关键代码，理解“AI 提想法，Harness 当安全守门人”的职责分离。在操作过程中，我发现并修正了命令行路径和环境方面的问题，最后确认演示和测试能按预期运行。

整个过程中，AI 更像是快速生成原型和解释概念的伙伴，而我负责运行项目、验证行为、理解设计并完成反思。对于自己还不能完全解释的代码，我没有把它写成自己独立完成的成果。

## 四、安全边界与项目不足

我觉得当前项目最重要的安全边界，就是 Harness 在执行工具动作前检查命令和文件路径。它相当于 AI 和操作系统之间的一道门禁，安全不是靠“信任 AI 不乱来”，而是靠代码强制检查。

但它目前还不能保证的事情也很明显。首先，这不是一个真正的沙箱。程序主要通过命令分词后的危险 token 检查和路径比对来拦截操作，实际执行仍然在宿主操作系统上完成。如果检查逻辑有漏洞，或者遇到没有考虑到的边界情况，危险命令仍然可能漏过去。

其次，人工确认环节目前只是预留了接口，实际运行时遇到危险动作会直接拒绝，没有真正弹出交互让用户审批。因此，对于确实有风险但用户知情后愿意承担的操作，目前还不能灵活处理。

另外，配置和凭据管理代码已经有了雏形，但还没有经过真实环境检验。这次没有配置真实 API key，只在代码层面理解了相关设计。未来对接多个 LLM 供应商或跨机器分发时，还需要验证密钥管理和跨平台运行。

## 五、冷启动验证

原来我觉得 SPEC 和 PLAN 已经写得挺清楚了，目标、架构和模块分工都有，自己看着是能理解的。但另一个智能体只凭这两份文档没有顺利完成任务，最后超时了。这让我意识到，对人够用的文档，对智能体可能还差很多东西。

文档里写了“做什么”和“大概怎么做”，但可能缺少项目已有代码风格、具体函数签名和测试运行方式等上下文。人会自动补全这些信息，但智能体不会。

这次经历让我认识到，智能体不像经验丰富的同事，更像一个需要明确指令的实习生。任务必须拆细，上下文必须完整，而且一定要有验证机制。冷启动超时说明自主运行有边界，还需要超时和产出检查等兜底手段。

## 六、凭据、分发与后续改进

这次没有配置真实 API key，所以凭据管理主要是在代码层面理解，没有跑通完整流程。从 `credentials.py` 的设计来看，密钥、端点和模型名称等敏感信息应当和业务代码分离，通过环境变量或操作系统凭据管理器管理，而不是硬编码进源码。以前我写小脚本时容易直接把 key 写在代码里，这次才意识到代码一旦分享或上传到公开仓库，这种做法会带来安全隐患。

从 `pyproject.toml` 和 README 中，我也看到项目可以打包成标准 Python 包，通过命令行入口使用。分发后还要考虑用户如何安全配置密钥。

如果重新做一次，我会把任务拆得更小、更具体，例如把安全检查拆成危险命令检查、路径越界检查和人工审批占位三个步骤，每完成一小步就运行测试。我还会让 SPEC 和 PLAN 写得更具体，补充函数签名、文件路径、代码风格、测试命令和预期输出。除此之外，我会增加更多边界和异常测试，例如大小写混用、符号链接以及合法和非法操作交替出现的情况。

我还希望把 mock LLM 替换成真实 LLM 或本地小模型，并增加终端交互界面。不过这些改进不能替代确定性测试，因为移除真实 LLM 后，Harness 的核心安全机制仍然应该可以独立验证。
